{
	"name": "lychee",
	"version": "1.0.0",
	"description": "lychee client",
	"private": true,
	"workspaces": {
		"packages": [
			"packages/*",
			"app-*"
		]
	},
	"scripts": {
		"bootstrap": "lerna bootstrap",
		"relay": "lerna run relay",
		"watch": "lerna run watch",
		"build": "lerna run build",
		"clean": "lerna run clean && rm -rf app-*/node_modules && rm -rf ./node_modules ./dist && rm -rf yarn.lock",
		"flush": "lerna run flush && rm -rf ./dist",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"lerna": "^3.16.4"
	},
	"author": "Sébastien d'Olbecke <sebastien@dolbecke.com>",
	"license": "unlicenced"
}
